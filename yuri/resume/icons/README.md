# Ícones do currículo

A página web (`/yuri/resume/`) e o PDF gerado já usam **ícones SVG inline** — não precisam destes PNGs.

Estes PNGs são só para compilar a **fonte LaTeX** (`resume.tex` / `resume-pt.tex`) no Overleaf,
que faz `\includegraphics` a partir desta pasta (`\graphicspath{{icons/}}`). Coloque aqui:

- `github.png`
- `linkedin.png`
- `site.png`   (ícone do artelonga.com.br)

Fundo transparente, ~64×64 px. Se quiser usar SEUS PNGs também na página web, me avise que troco os SVG por `<img>`.
