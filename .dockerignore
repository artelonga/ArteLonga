web/node_modules
web/.svelte-kit
web/build
.git
.DS_Store
CLAUDE.md
README.md
design/
.obsidian/
data/
*.pptx

# build-context bloat — surfaces image só precisa de tools/surfaces-server.mjs + yuri/ (servido).
# Estes não são servidos por nenhuma superfície; cortam ~550M do contexto/imagem → deploy bem mais rápido.
node_modules
yuri/neuro/IMG_*.png
playwright-report
test-results
tests
# geo é DATA de runtime (não content+form): NÃO entra na imagem via context —
# é baixado+compilado no build (Dockerfile RUN bake-geo). Deploy reproduzível
# independente do estado local.
yuri/geo/*.bin
