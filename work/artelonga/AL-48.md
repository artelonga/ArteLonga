---
id: 48
title: "Analytics activation pipeline (após CO-177/178/179/180)"
type: user-story
status: done
priority: medium
labels:
  - feat
  - analytics
  - integration
module: assets
unblocked: CO-177, CO-178, CO-179, CO-180 todas mergeadas (2026-05-11). Todas as 4 sub-tasks executáveis agora.
created_at: 2026-05-10T00:00:00Z
updated_at: 2026-05-11T18:36:44.143347+00:00
supersedes: [32, 33, 34, 35]
---

GIVEN analytics.js tem `ENDPOINT = ""` (events queue em localStorage,
nunca enviados), AND `/analytics/` dashboard falla porque endpoints
backend `co` ainda não existem (CO-177 CORS + CO-179 public summary),
AND ranking de services no home por popularidade depende de
`popularity.json` que vem de `co` aggregation (CO-180), AND geo data
no dashboard depende de IP enrichment server-side (CO-178),

WHEN CO-177/178/179/180 deployam, AND nós (a) setamos
`ENDPOINT = "https://co.artelonga.com.br/api/v1/telemetry/events"`,
(b) adicionamos `.github/workflows/bake-popularity.yml` que fetcha
endpoint daily + commita `assets/popularity.json`, (c) atualizamos
`src/pages/home.ts` pra sort por views desc usando popularity.json,
(d) verificamos `/analytics/` dashboard renderiza dados reais (backend
endpoints retornam shape esperado),

THEN telemetria flui em tempo real, ranking empírico replaces alfabético,
dashboard mostra geo + recent events + KPIs reais.

---

## Status: blocked

Esta task agrega 4 sub-tasks que dependem TODAS de backend `co`
mergear primeiro. Não tocar até verify:

- CO-177 — CORS allowlist artelonga.com.br + populate universe_key no marketing handler
- CO-178 — geo enrichment (country + city) no telemetry_events
- CO-179 — public analytics endpoints (`/api/v1/analytics/public/summary` + `/recent`)
- CO-180 — popularity endpoint (`/api/v1/analytics/public/popularity?prefix=&days=`)

Quando essas mergearem, flip status pra `todo` e executa as 4 sub-tasks
abaixo num único PR (todas relacionadas, faz sentido bundlar — mesmo
PR de "ativação analytics").

## Sub-task 1: Wire `analytics.js` ENDPOINT

```diff
- const ENDPOINT = "";
+ const ENDPOINT = "https://co.artelonga.com.br/api/v1/telemetry/events";
```

V bump. Smoke test: hard reload `/`, DevTools Network mostra POST a co
retornando 200/204. `co.artelonga.com.br/admin/telemetry` mostra
novo event.

## Sub-task 2: GH Action bake-popularity

`.github/workflows/bake-popularity.yml`:

```yaml
name: bake-popularity
on:
  schedule: [{ cron: '0 4 * * *' }]
  workflow_dispatch: {}
permissions: { contents: write }
jobs:
  bake:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          curl -fsSL "https://co.artelonga.com.br/api/v1/analytics/public/popularity?prefix=/servicos/&days=30" \
            | jq -S '.' > assets/popularity.json
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: bake popularity.json"
          file_pattern: assets/popularity.json
```

## Sub-task 3: Renderer ranking

`src/pages/home.ts`:

```ts
const popularity = await fetch('/assets/popularity.json')
    .then(r => r.ok ? r.json() : { items: [] })
    .catch(() => ({ items: [] }));
const byPath = new Map(popularity.items.map(i => [i.path, i.views]));

const topo = AL.publicServices()
    .filter(s => !s.parent)
    .sort((a, b) => {
        const va = byPath.get(`/servicos/${a.slug}/`) || 0;
        const vb = byPath.get(`/servicos/${b.slug}/`) || 0;
        if (va !== vb) return vb - va;
        return a.titulo.localeCompare(b.titulo, 'pt-BR');
    });
```

Fallback: missing/empty popularity.json → alfabético (sem crash).

## Sub-task 4: `/analytics/` smoke test

Após CO-179 deploy, verificar `/analytics/` dashboard:
- KPIs preenchidos
- Timeseries SVG renderiza
- Top pages tabela
- Geo seção (depende CO-178)
- Recent events
- Fallback localStorage ainda ativo se backend falhar

Se shape divergir do que `analytics/index.html` consome, ajustar URLs
ou JS no frontend.

## Critérios de aceitação (quando desbloquear)

- [ ] CO-177/178/179/180 todas mergeadas + deployadas em prod.
- [ ] `analytics.js` ENDPOINT ativo, POST events visíveis em backend.
- [ ] `.github/workflows/bake-popularity.yml` rodando + commit nightly.
- [ ] `assets/popularity.json` populado e ordenado.
- [ ] Home grid ordenado por views desc com tie-break alfabético.
- [ ] Fallback graceful se popularity missing.
- [ ] `/analytics/` renderiza dados reais (smoke test manual).
- [ ] CHANGELOG entry.
- [ ] V bumped.

## Out of scope

- Customer-facing analytics page (only admin/dev).
- Real-time updates (websocket).
- A/B testing framework — separate AL.
- Cookie consent banner — separate AL (LGPD compliance).

## Related

- CO-177, CO-178, CO-179, CO-180 — backend deps.
- AL-22 — analytics endpoints design.
