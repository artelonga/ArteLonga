---
id: 55
title: "OpenAPI codegen for src/types.ts"
type: user-story
status: done
priority: low
conventional_commit: "feat(types):"
semver_bump: minor
labels:
  - type:feat
  - module:types
module: site
parent: 66
created_at: 2026-05-18T00:00:00Z
updated_at: 2026-05-18T00:00:00Z
---

## As

A maintainer of artelonga.com.br

## I Need

`src/types.gen.ts` generated from `openapi/artelonga.yaml` by `openapi-typescript`

## So That

A known drift surface closes; CLAUDE.md's TODO note for codegen is resolved.

## Context

- **Principles:** §3 (static typing), §4 (reduced coupling)
- **Scope:** Add `openapi-typescript` devDep. `npm run gen-types` produces `src/types.gen.ts`. Replace hand-mirrored `src/types.ts` with a re-export from the generated file (or delete entirely, importing from `src/types.gen.ts`).

## Acceptance

- `tsc --noEmit` passes against generated types.
- Hand-written `src/types.ts` removed or reduced to UI-only types not in the OpenAPI.
- `npm run gen-types` is part of `npm run bake`.
- Pre-commit hook checks `types.gen.ts` is in sync with `openapi/artelonga.yaml`.

## Blast radius

Low — types only, no runtime change.
