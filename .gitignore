# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# SvelteKit
.svelte-kit/
build/

# Env
.env
.env.*
!.env.example

# Runtime data (SQLite + uploads)
web/data/
*.db
*.db-shm
*.db-wal

# co-web cache/metadata
.co/

# Claude local config
.claude/

# IDE
.vscode/
.idea/

# Office / binary drafts (not for public repo)
*.pptx
*.xlsx
*.docx
*.key
*.numbers
*.pages

# Obsidian
.obsidian/

# Vite build artifacts
dist/

# Playwright / Lighthouse artifacts
test-results/
playwright-report/
.lighthouseci/

# Temporary / scratch
tmp/
scratch/
*.tmp
*.bak
*.swp
.worktrees/

# geo bins são DATA de runtime (não content+form) — baixados+compilados no build
# (Dockerfile RUN bake-geo). País (CC0) e cidade (DB-IP CC-BY) ficam fora do git.
yuri/geo/*.bin

# security review artifact — mapa de superficie exposta, NUNCA servir/commitar
_security-review.html
